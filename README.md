# MyGameEngine

Игровой движок на C++ с использованием OpenGL для рендеринга.

## 🚀 Возможности

- **Графический рендеринг**: OpenGL 3.3 Core с поддержкой PBR материалов
- **Система сцен**: Управление объектами и камерами
- **Загрузка моделей**: Поддержка различных форматов через Assimp
- **Система ввода**: Обработка клавиатуры и мыши через GLFW
- **Математика**: Векторы, матрицы, кватернионы
- **Логирование**: Многоуровневая система логирования

## 📋 Требования

- **CMake** 3.15 или выше
- **C++17** компилятор (MSVC, GCC, Clang)
- **OpenGL** 3.3 или выше
- **Windows** (текущая платформа)

## 🔧 Сборка

### Windows (Visual Studio)

```bash
mkdir build
cd build
cmake .. -G "Visual Studio 17 2022" -A x64
cmake --build . --config Release
```

### Linux/Mac

```bash
mkdir build
cd build
cmake ..
make -j4
```

## 🎮 Использование

После сборки запустите исполняемый файл:

```bash
./build/Release/MyGameEngine.exe  # Windows
./build/MyGameEngine               # Linux/Mac
```

### Управление

- **ESC** - Выход
- **SPACE** - Переключение wireframe режима
- **R** - Сброс камеры

## 📁 Структура проекта

```
engine/
├── src/
│   ├── Core/          # Ядро движка (Engine, Log, Time, Events)
│   ├── Graphics/      # Графическая подсистема (Renderer, Mesh, Shader, Camera)
│   ├── Math/          # Математические типы (Vector, Matrix, Quaternion)
│   ├── Input/         # Система ввода
│   └── main.cpp       # Точка входа
├── external/          # Внешние библиотеки
│   ├── glfw/         # Оконная система
│   ├── glad/         # OpenGL загрузчик
│   ├── assimp/       # Загрузка моделей
│   └── stb/          # Утилиты (загрузка изображений)
├── assets/           # Ресурсы (текстуры, модели, шейдеры)
├── CMakeLists.txt    # Конфигурация сборки
└── README.md         # Этот файл
```

## 🛠️ Технологии

- **OpenGL 3.3** - Графический API
- **GLFW** - Создание окон и обработка ввода
- **GLAD** - Загрузка OpenGL функций
- **Assimp** - Импорт 3D моделей
- **STB** - Загрузка изображений

## 📝 Пример кода

```cpp
#include "Core/Engine.h"
#include "Graphics/Graphics.h"

class MyApp : public Engine {
protected:
    void OnInitialize() override {
        Renderer::Init();
        // Инициализация вашего приложения
    }
    
    void OnUpdate(f32 deltaTime) override {
        // Обновление логики
    }
    
    void OnRender() override {
        // Рендеринг
    }
};

int main() {
    MyApp app;
    app.Run();
    return 0;
}
```

## 🐛 Отладка

Логи сохраняются в файл `engine.log` (если включено файловое логирование).

Уровни логирования:
- `ENGINE_TRACE` - Детальная отладочная информация
- `ENGINE_DEBUG` - Отладочная информация
- `ENGINE_INFO` - Информационные сообщения
- `ENGINE_WARN` - Предупреждения
- `ENGINE_ERROR` - Ошибки
- `ENGINE_CRITICAL` - Критические ошибки

## 📚 Документация

Подробные предложения по улучшению см. в файле [IMPROVEMENTS.md](IMPROVEMENTS.md).

## 🤝 Вклад

Проект находится в активной разработке. Предложения и исправления приветствуются!

## 📄 Лицензия

[Укажите вашу лицензию]

## 🔗 Ссылки

- [OpenGL Documentation](https://www.khronos.org/opengl/)
- [GLFW Documentation](https://www.glfw.org/documentation.html)
- [Assimp Documentation](https://assimp-docs.readthedocs.io/)

